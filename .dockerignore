# Dependencies — reinstalled inside the container
node_modules/

# Build output — regenerated during the Docker build
dist/

# Local secrets — inject via env vars at runtime, never bake into the image
.env

# Dev / editor artifacts
.git/
*.log
.DS_Store
